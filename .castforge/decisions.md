# Decisions

## 2026-08-15

- system: {"kind":"state_digest_seed_applied","phase_title":"","section_count":2}
- coder: Card b9f233f5 'Build the application image'. This is a Cloudflare Workers app (vinext + @cloudflare/vite-plugin) whose app code depends on env.DB (D1) and provider-key bindings. I traced vinext's prod server and confirmed `vinext start` invokes the worker with env=undefined (no bindings) — so serving via the Node prod server would break all DB features. Chose instead to serve the *built* worker under workerd via `wrangler dev --local`: `npm run build` emits dist/server/wrangler.json (DB binding + assets dir), which the runtime CMD targets, binding 0.0.0.0:8787 and persisting local D1 to /app/.wrangler/state. Authored a multi-stage Dockerfile (Debian bookworm-slim because workerd needs glibc): builder stage runs `npm ci` + `npm run build`; runner stage copies node_modules+dist for exact locked reproducibility, creates non-root appuser (uid 10001), declares the .wrangler/state path writable, and disables wrangler telemetry. Added .dockerignore that excludes node_modules/dist/.wrangler and secrets (.dev.vars, .env*) while deliberately keeping .openai/hosting.json (required by vite.config). Verified locally end-to-end: image builds clean, container returns GET / HTTP 200 with DB-backed page + assets, D1 sqlite created under persisted path owned by appuser. Left compose/runtime-config/docs to their dedicated follow-on cards.
- system: {"kind":"state_digest_seed_applied","phase_title":"","section_count":3}
- system: {"kind":"state_digest_seed_applied","phase_title":"","section_count":1}
- coder: Created compose.yaml defining a single `web` service that builds the existing Dockerfile (image promptforge:local). It adds the pieces a single-container run needs to act like a real local deployment: a named volume promptforge-d1 at /app/.wrangler/state so the local D1 database survives down/up; a Node-based healthcheck (runtime image has Node 22 with global fetch) treating <500 as healthy so `up --wait` blocks until the worker answers; restart: unless-stopped; overridable HOST_PORT; and an optional .dev.vars env_file (required:false). I deliberately scoped OUT actually binding env vars into the Worker runtime because wrangler resolves .dev.vars relative to its config dir (dist/server) and does NOT auto-bind arbitrary process env to Worker vars — that env->Worker bridge is the very next card ('Adapt runtime configuration for containers'), which depends on this one, so env_file only loads keys into the container environment as a clean hand-off. Verified on live Docker: compose config valid, up --build --wait reached Healthy, curl returned HTTP 200, docker inspect health=healthy, D1 sqlite persisted with identical MD5 checksum across a full down/up. Also added compose files to .dockerignore so they don't enter the build context.
- system: {"kind":"state_digest_seed_applied","phase_title":"","section_count":3}
- system: {"kind":"state_digest_seed_applied","phase_title":"","section_count":1}
- coder: The card required making provider keys + SIGNUP_INVITE_CODE that reach the container (compose env_file / docker run -e) actually available to the app. Root cause: the worker reads these via `env` from cloudflare:workers, but workerd under `wrangler dev --local` does not inherit process.env — wrangler loads secrets only from a `.dev.vars` file resolved next to the `-c` config (dist/server/.dev.vars), which the image doesn't ship. Fix: added docker-entrypoint.sh that, on each start, writes dist/server/.dev.vars from an explicit allowlist (ANTHROPIC_API_KEY OPENAI_API_KEY GOOGLE_API_KEY SIGNUP_INVITE_CODE), skipping empties, then execs the wrangler CMD. Chose an allowlist over wrangler's CLOUDFLARE_INCLUDE_PROCESS_ENV passthrough so unrelated container vars never leak into the Worker (least-exposure, ahead of the security review card). Wired ENTRYPOINT [sh, /app/docker-entrypoint.sh] in the Dockerfile (invoked via sh to survive a lost exec bit on Windows), kept the wrangler CMD unchanged. Added .gitattributes pinning *.sh to eol=lf because core.autocrlf=true would otherwise corrupt the script to CRLF and break sh on a fresh checkout. Updated the compose.yaml comment that had deferred this. Verified end-to-end on a real docker build: allowlisted keys land in .dev.vars and wrangler reports loading them, an unrelated var is excluded, no-keys yields an empty file with the app still serving HTTP 200, and restart regenerates the file — all HTTP 200.
- system: {"kind":"state_digest_seed_applied","phase_title":"","section_count":3}
- system: {"kind":"state_digest_seed_applied","phase_title":"","section_count":1}
- coder: Added a '## Docker' section to README.md covering how to run PromptForge in a container. It documents: (1) the recommended Compose path (`docker compose up --build --wait`, logs, `down`, `down -v`, and `HOST_PORT` override); (2) the equivalent plain `docker build`/`docker run` invocation with explicit port, `--env-file`, and volume; (3) how provider keys and SIGNUP_INVITE_CODE reach the Worker — clarifying they're read from the Worker `env`, not the process env, and that docker-entrypoint.sh bridges the gap by writing an allowlisted set into `.dev.vars` on every start; (4) data persistence via the `promptforge-d1` named volume mounted at /app/.wrangler/state. WHY: the card called for documenting the Docker workflow, and the README had no mention of the containerization work done in prior turns, leaving the Dockerfile/compose.yaml/entrypoint undocumented for the team. Every claim was verified against the actual files (volume name, HOST_PORT default, env_file, allowlist vars, port 8787, down -v behavior). Docs-only change, no runtime surface to verify.
- system: {"kind":"state_digest_seed_applied","phase_title":"","section_count":3}
- system: {"kind":"state_digest_seed_applied","phase_title":"","section_count":1}
- reviewer: As Reviewer for card 45b5f418, audited Dockerfile, compose.yaml, docker-entrypoint.sh, .dockerignore, .gitattributes, and README Docker section. Verified security posture: non-root appuser (uid 10001, nologin), secrets excluded from build context/image via .dockerignore, runtime-only .dev.vars generation with a 4-var allowlist matching the app's actual env reads (lib/models.ts, lib/auth.ts), safe printf %s usage, set -eu. Verified two non-obvious correctness claims against wrangler 4.92.0 source: (1) wrangler resolves .dev.vars from dirname(configPath)=dist/server, exactly where the entrypoint writes it (getVarsForDev); (2) WRANGLER_LOG_PATH without a .log suffix is treated as a directory (getDebugFilepath), so mkdir -p .../logs is correct. Multi-stage build reuses locked node_modules for reproducibility; .gitattributes pins LF on shell scripts. No security or maintainability defects found — APPROVE.
- Reviewer: castforge_task: 45b5f418-fbab-45e4-a16c-d464010265fa doingLet me verify the claims against the actual application code.Let me verify where wrangler resolves `.dev.vars` from, since the entrypoint writ
- system: {"kind":"state_digest_seed_applied","phase_title":"","section_count":2}
- coder: This was a verification-only card (Container verification phase). To prove the 'from scratch' claim honestly, I exported only the tracked + relevant untracked files into a scratch dir /tmp/pf-clean — deliberately excluding node_modules, dist, .wrangler, .vinext and the pf*.tar.gz snapshots — so the Docker build context had no pre-built local state to lean on. Then I drove the full lifecycle with an isolated compose project (-p pfverify, HOST_PORT=8799): (1) BUILD — `docker compose up --build --wait --wait-timeout 300` built the multi-stage image clean and blocked until the healthcheck passed; (2) EXERCISE — `GET /` returned HTTP 200 with the real D1-backed <title>PromptForge</title> page (11207 bytes), and the local D1 sqlite was provisioned under /app/.wrangler/state owned by appuser (uid 10001), confirming the non-root writable-volume design; (3) PERSISTENCE — wrote a sentinel table `_verify(note='survives-restart')` via in-container `wrangler d1 execute DB --local`, ran `compose down` (volume kept), then `compose up --wait` back to Healthy, and confirmed the sentinel row survived the stop/restart and the app still served 200; (4) CLEAN SLATE — `compose down -v` wiped the volume and a fresh `up --build` came up Healthy with the sentinel table absent but the app's OWN tables (users, sessions, packs, pack_versions, runs) present — proving the schema auto-provisions on first serve. Finally tore down the stack (down -v) and removed the scratch dir. WHY it matters: this closes the docker-verification phase's core question with real command output, and surfaces two operational facts for the team — the named volume round-trips correctly (persist on down/up, clean on down -v), and a new operator needs no manual migration step because the app self-provisions its D1 schema. No source changes were needed; the artifacts from the earlier build/wire cards work as designed against a genuinely clean checkout.
- system: {"kind":"state_digest_seed_applied","phase_title":"","section_count":3}
- coder: Executed the 'Run container smoke checks' card against a clean slate (docker compose down -v first). Verified with real Docker (Engine 29.7.2, Compose v5.3.1): (1) BUILD — `docker compose up --build --wait --wait-timeout 240` built promptforge:local and the container reached Healthy via the compose healthcheck, running non-root as uid=10001 appuser on 0.0.0.0:8787. (2) EXERCISE — GET / returned HTTP 200 with the real PromptForge page; a hashed JS asset returned 200 text/javascript; an unknown path returned 404 (routing OK); GET /api/auth returned 200 with signup.bootstrap:true proving D1 was provisioned/read on first request; POST /api/auth signup returned 201 creating the owner account (D1 write), and a follow-up GET showed bootstrap:false/open:false confirming the write persisted and signup auto-closed. The D1 SQLite file was physically created on the persisted volume (69KB, owned by appuser). Container logs were clean — no errors/exceptions. (3) STOP — `docker compose stop` produced a clean Exited(0), the port stopped answering (connection refused as expected), and `docker compose down` removed container+network. WHY it matters: this proves a clean checkout builds, boots healthy, serves HTTP + static assets + D1-backed read/write end-to-end, and stops cleanly under the container runtime. No code changes were needed; this is a pure verification card and everything passed.
- system: {"kind":"state_digest_seed_applied","phase_title":"","section_count":3}
- coder: Ran the full container lifecycle against a clean checkout using the committed Dockerfile/compose.yaml/docker-entrypoint.sh. (1) `docker compose down -v` then `up --build --wait` built the image and reached Healthy; `GET /` returned 200. (2) Exercised the DB by creating the owner account via POST /api/auth action=signup (201, role=owner) — confirmed the D1 sqlite was written under the persisted volume path /app/.wrangler/state/v3/d1/miniflare-D1DatabaseObject/*.sqlite. (3) Persistence across restart: `docker compose down` (volume kept) then `up --wait`, then login with the pre-restart credentials returned the SAME user id usr_2726c8aed9994746a48c — proving the D1 row survived a full container recreate. (4) Restart policy is unless-stopped; an in-place `docker compose restart` kept the container Healthy, login still succeeded, and the entrypoint regenerated dist/server/.dev.vars (empty, since no provider keys were set) confirming the env->Worker bridge re-runs every start. (5) Clean-slate semantics: `down -v` removed the named volume; after `up` the old login failed 401 and GET /api/auth reported signup bootstrap:true/open:true, proving the DB was genuinely wiped. (6) Left the environment torn down clean (down -v). WHY: this card only asked to prove the built/started/exercised/stopped/restarted lifecycle and persistence; the implementation from prior cards was correct, so no diffs were warranted. IMPACT: the docker-verification phase's restart+persistence acceptance is satisfied; the container stack is confirmed production-of-local-dev ready.
- system: {"kind":"state_digest_seed_applied","phase_title":"","section_count":2}
- reviewer: Independently reviewed the full Docker surface (Dockerfile, compose.yaml, docker-entrypoint.sh, .dockerignore, .gitattributes, README Docker section) for the final readiness card. Confirmed the entrypoint allowlist (ANTHROPIC_API_KEY, OPENAI_API_KEY, GOOGLE_API_KEY, SIGNUP_INVITE_CODE) exactly matches every env key the app reads — the three provider envKeys in lib/models.ts and SIGNUP_INVITE_CODE in lib/auth.ts (via lib/env.ts envString/hasEnv); no consumed key missing, no unrelated var leaked. Verified env.DB/env.ASSETS are Worker bindings present in dist/server/wrangler.json, and db/ensure.ts creates tables on first request (no migration step), matching docs. Confirmed .dev.vars is written to dirname(config)=dist/server where wrangler -c resolves it. Security posture sound: non-root appuser, secrets excluded from build context, .dev.vars truncated+regenerated each start and kept out of the persisted volume, set -eu, safe printf. .gitattributes pins shell scripts to LF. Prior implementation cards all passed live Docker verification (build clean, HTTP 200, D1 MD5 identical across down/up, key forwarding, no-keys boots, restart regenerates). Phase goal (build/start/exercise/stop/restart a clean checkout via Docker) is met. Verdict: APPROVE.
- Reviewer: **Correctness — verified against source:**
